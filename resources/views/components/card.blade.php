<div class="card shadow mb-4 {{empty($class) ? '' : $class}}">
  <div class="card-header">
    <h6 class="text-primary m-0 font-weight-bold">{{$title}}</h6>
  </div>
  <div class="card-body">
      {{$body}}
  </div>
</div>